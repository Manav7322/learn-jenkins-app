pipeline {
    agent any

    stages {
        stage('build') {
            agent {
                docker {
                    image 'node:18'
                    reuseNode true
                }
            }

            steps {
                sh '''
                    set -e

                    ls -la
                    node --version
                    npm --version

                    npm install
                    npm run build
                '''
            }
        }
    }
}
